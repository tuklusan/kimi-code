name: Sanyalnet E2E Fibonacci

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "workflow parses"
